#pragma once

#include "canbus_project.h" /**< Required for subModule_t */
#include "node_state.h"     /**< Required for node and trackers */

/** Producer tick counter */
extern uint32_t lastProducerTick[]; /**< Array of producer tick counters */
